<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:FarmOrganizer.ViewModels"
             x:Class="FarmOrganizer.Views.DebugPage"
             x:DataType="viewmodel:DebugPageViewModel"
             Title="DebugPage">
    
    <ScrollView>
        <VerticalStackLayout>
            <Button Command="{Binding PerformCRUDTestsCommand}" Text="Do the thing!"/>
            <Label Text="{Binding DebugText}"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>