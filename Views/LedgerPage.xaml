<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:FarmOrganizer.ViewModels"
             x:Class="FarmOrganizer.Views.LedgerPage"
             x:DataType="viewmodel:LedgerPageViewModel"
             Title="Tabela kosztów">

    <Grid RowDefinitions=".1*,*,.15*"
          Padding="10">
        <VerticalStackLayout>
            <Label Text="Wybrane pole:"/>
            <Picker></Picker>
        </VerticalStackLayout>
        <SwipeItemView Grid.Row="1">

        </SwipeItemView>
        <HorizontalStackLayout Grid.Row="2" HorizontalOptions="Center">
            <Button Text="Dodaj wpis" Margin="2"/>
            <Button Text="Generuj raport i oblicz" Margin="2"/>
        </HorizontalStackLayout>
    </Grid>
</ContentPage>